---
date: 2022-09-18T12:00:00
type: post
category: tutorial
post_number: 1
highlighted: true
title: How to build an animated SVG doughnut chart
description: Have you ever needed to make an animated doughnut chart? There's no need to use a bloated third-party library! Here's a quick guide to walk you through building your own SVG based doughnut chart, and how to animate it yourself with pure CSS!
---

# How to build an animated SVG doughnut chart

Step one, you make it.
Step Two, you animate it.
Step Three, you eat it.

## Step one: Make it

First, we need to make a doughnut chart. We'll be using SVG for this, so we'll need to create an SVG element. We'll also need to create a circle element, and a path element. The circle element will be the background of the doughnut chart, and the path element will be the actual doughnut chart.

```html
<svg width="100" height="100">
	<circle cx="50" cy="50" r="50" fill="#eee" />
	<path fill="#000" />
</svg>
```

### I should be a sub element

Now we need to make the doughnut chart itself. We'll do this by using the `stroke-dasharray` and `stroke-dashoffset` properties. The `stroke-dasharray` property is the length of the stroke, and the `stroke-dashoffset` property is the offset of the stroke. We'll use these properties to make the doughnut chart.

```html
<svg width="100" height="100">
	<circle cx="50" cy="50" r="50" fill="#eee" />
	<path fill="#000" stroke-dasharray="314.1592653589793" stroke-dashoffset="0" />
</svg>
```

The `stroke-dasharray` property is the length of the stroke, and the `stroke-dashoffset` property is the offset of the stroke. We'll use these properties to make the doughnut chart.

```html
<svg width="100" height="100">
	<circle cx="50" cy="50" r="50" fill="#eee" />
	<path fill="#000" stroke-dasharray="314.1592653589793" stroke-dashoffset="0" />
</svg>
```

## Step two: Animate it

Now that we have our doughnut chart, we need to animate it. We'll do this by using the `stroke-dashoffset` property. The `stroke-dashoffset` property is the offset of the stroke. We'll use this property to animate the doughnut chart.

```html
<svg width="100" height="100">
	<circle cx="50" cy="50" r="50" fill="#eee" />
	<path fill="#000" stroke-dasharray="314.1592653589793" stroke-dashoffset="0" />
</svg>
```

## Step three: Eat it

Now that we have our doughnut chart, we need to eat it. We'll do this by using the `stroke-dashoffset` property. The `stroke-dashoffset` property is the offset of the stroke. We'll use this property to animate the doughnut chart.

```html
<svg width="100" height="100">
	<circle cx="50" cy="50" r="50" fill="#eee" />
	<path fill="#000" stroke-dasharray="314.1592653589793" stroke-dashoffset="0" />
</svg>
```

## Conclusion

Now that we have our doughnut chart, we need to eat it. We'll do this by using the `stroke-dashoffset` property. The `stroke-dashoffset` property is the offset of the stroke. We'll use this property to animate the doughnut chart.

```html
<svg width="100" height="100">
	<circle cx="50" cy="50" r="50" fill="#eee" />
	<path fill="#000" stroke-dasharray="314.1592653589793" stroke-dashoffset="0" />
</svg>
```
